# Copy to `.env` and adjust for each machine (`.env` is usually gitignored).

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB=snh6_swiftproject

SECRET_KEY=change-me-in-production
JWT_SECRET_KEY=change-me-in-production
